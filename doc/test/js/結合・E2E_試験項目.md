# フロントエンド 結合試験・E2E試験 試験項目表

> **実施方法：** Spring Boot を起動した状態でブラウザから手動確認
> **証跡：** 各項目の結果欄に ✅ / ❌ を記入し、Gitにコミットして保管

---

## 結合試験

モジュール間の連携（バリデーション → API呼び出し → 画面反映）を検証する。

### メモ：新規作成フロー

| No. | 試験内容                                           | 手順                                                                   | 期待結果                                                             | 結果 |
| --- | -------------------------------------------------- | ---------------------------------------------------------------------- | -------------------------------------------------------------------- | ---- |
| 1   | 正常な入力で保存できる                             | ① 新規作成ボタンを押す<br>② タイトル・内容を入力<br>③ 保存ボタンを押す | モーダルが閉じ、テーブルに新しい行が追加される                       | ok   |
| 2   | 保存成功時にトーストが表示される                   | ① 正常な入力で保存する                                                 | "メモが作成されました" のトーストが表示される                        | OK   |
| 3   | タイトル空欄のままでは保存できない                 | ① タイトルを空のまま保存ボタンを押す                                   | モーダルが閉じず、タイトル欄が赤枠になりエラーメッセージが表示される | OK   |
| 4   | バリデーションエラー後に正しく入力すると保存できる | ① 空欄で保存を試みる<br>② タイトルを入力して再度保存する               | エラーが消え、正常に保存される                                       | OK   |

### メモ：編集フロー

| No. | 試験内容                         | 手順                                                 | 期待結果                                               | 結果 |
| --- | -------------------------------- | ---------------------------------------------------- | ------------------------------------------------------ | ---- |
| 5   | 編集モーダルに既存値が入っている | ① テーブル行の編集ボタンを押す                       | モーダルにその行のタイトル・内容が入力済みで表示される | OK   |
| 6   | 編集内容が保存される             | ① 編集ボタンを押す<br>② タイトルを変更<br>③ 保存する | テーブルの該当行が更新された値になる                   | OK   |
| 7   | 編集成功時にトーストが表示される | ① 編集を保存する                                     | "メモが更新されました" のトーストが表示される          | OK   |

### メモ：削除フロー

| No. | 試験内容                                           | 手順                                                 | 期待結果                              | 結果 |
| --- | -------------------------------------------------- | ---------------------------------------------------- | ------------------------------------- | ---- |
| 8   | 削除するとテーブルから消える                       | ① 削除ボタンを押す<br>② 確認ダイアログで OK を押す   | 該当行がテーブルから消える            | OK   |
| 9   | 削除成功時にトーストが表示される                   | ① 削除を実行する                                     | "削除しました" のトーストが表示される | OK   |
| 10  | 削除の確認ダイアログでキャンセルすると削除されない | ① 削除ボタンを押す<br>② ダイアログでキャンセルを押す | 行が残ったままになる                  | OK   |

### 日誌：新規作成フロー

| No. | 試験内容                                 | 手順                                                         | 期待結果                                           | 結果 |
| --- | ---------------------------------------- | ------------------------------------------------------------ | -------------------------------------------------- | ---- |
| 11  | 正常な入力で保存できる                   | ① 新規作成ボタンを押す<br>② 全フィールドを入力<br>③ 保存する | モーダルが閉じ、テーブルに新しい行が追加される     | OK   |
| 12  | 新規作成モーダルに今日の日付が入っている | ① 新規作成ボタンを押す                                       | 作業日フィールドに本日の日付が自動入力されている   | OK   |
| 13  | 保存成功時にトーストが表示される         | ① 正常な入力で保存する                                       | "日誌が作成されました" のトーストが表示される      | OK   |
| 14  | タイトル空欄では保存できない             | ① タイトルを空のまま保存する                                 | タイトル欄が赤枠になりエラーメッセージが表示される | OK   |
| 15  | 作業時間に不正な値では保存できない       | ① 作業時間に "abc" を入力して保存する                        | 作業時間欄が赤枠になりエラーメッセージが表示される | OK   |
| 16  | 作業日未入力では保存できない             | ① 作業日を空にして保存する                                   | 作業日欄が赤枠になりエラーメッセージが表示される   | OK   |

### 日誌：編集フロー

| No. | 試験内容                         | 手順                                               | 期待結果                                                   | 結果 |
| --- | -------------------------------- | -------------------------------------------------- | ---------------------------------------------------------- | ---- |
| 17  | 編集モーダルに既存値が入っている | ① 編集ボタンを押す                                 | タイトル・内容・作業時間・作業日すべてに既存値が入っている | OK   |
| 18  | 編集内容が保存される             | ① 編集ボタンを押す<br>② タイトルを変更して保存する | テーブルの該当行が更新される                               | OK   |

### 日誌：削除フロー

| No. | 試験内容                     | 手順                                           | 期待結果                   | 結果 |
| --- | ---------------------------- | ---------------------------------------------- | -------------------------- | ---- |
| 19  | 削除するとテーブルから消える | ① 削除ボタンを押す<br>② ダイアログで OK を押す | 該当行がテーブルから消える | OK   |

### モーダル共通

| No. | 試験内容                                               | 手順                                                               | 期待結果                           | 結果 |
| --- | ------------------------------------------------------ | ------------------------------------------------------------------ | ---------------------------------- | ---- |
| 20  | モーダルを閉じると入力値がリセットされる               | ① モーダルを開いて値を入力<br>② 閉じるボタンで閉じる<br>③ 再度開く | 入力欄が空になっている             | OK   |
| 21  | モーダルを閉じるとバリデーションエラーがリセットされる | ① バリデーションエラーを発生させる<br>② モーダルを閉じて再度開く   | 赤枠・エラーメッセージが消えている | OK   |

---

## E2E試験

ユーザーの操作シナリオを通しで検証する。Spring Boot を起動した状態で実施。

### シナリオ 1：メモのCRUD一連操作

| No. | 手順                                                                               | 期待結果                                 | 結果 |
| --- | ---------------------------------------------------------------------------------- | ---------------------------------------- | ---- |
| 1   | アプリを起動し `/memo` にアクセスする                                              | メモ一覧画面が表示される                 |      |
| 2   | 新規作成ボタンを押し、タイトル「テストメモ」・内容「テスト内容」を入力して保存する | テーブルに「テストメモ」の行が追加される | OK   |
| 3   | 追加された行の編集ボタンを押し、タイトルを「更新メモ」に変更して保存する           | テーブルの行が「更新メモ」に更新される   | OK   |
| 4   | 「更新メモ」行の削除ボタンを押し、確認ダイアログで OK を押す                       | 「更新メモ」の行がテーブルから消える     | OK   |

### シナリオ 2：日誌のCRUD一連操作

| No. | 手順                                                       | 期待結果                       | 結果 |
| --- | ---------------------------------------------------------- | ------------------------------ | ---- |
| 5   | サイドバーの「日誌」をクリックする                         | 日誌一覧画面に切り替わる       |      |
| 6   | 新規作成ボタンを押し、全フィールドを入力して保存する       | テーブルに新しい行が追加される | OK   |
| 7   | 追加された行の編集ボタンを押し、タイトルを変更して保存する | テーブルの行が更新される       | OK   |
| 8   | 削除ボタンを押し OK を押す                                 | 行がテーブルから消える         | OK   |

### シナリオ 3：ページング操作

| No. | 手順                                             | 期待結果                                             | 結果 |
| --- | ------------------------------------------------ | ---------------------------------------------------- | ---- |
| 9   | 11件以上データがある状態で「次へ」ボタンを押す   | 2ページ目のデータが表示され、ページ番号が「2」になる | OK   |
| 10  | 「戻る」ボタンを押す                             | 1ページ目のデータに戻り、ページ番号が「1」になる     | OK   |
| 11  | 1ページ目では「戻る」ボタンが非表示になっている  | 「戻る」ボタンが表示されていない                     | OK   |
| 12  | 最終ページでは「次へ」ボタンが非表示になっている | 「次へ」ボタンが表示されていない                     | OK   |

### シナリオ 4：画面切り替え

| No. | 手順                                                                           | 期待結果                                   | 結果 |
| --- | ------------------------------------------------------------------------------ | ------------------------------------------ | ---- |
| 13  | サイドバーの「メモ」をクリックする                                             | メモ一覧が表示され、日誌一覧が非表示になる | OK   |
| 14  | サイドバーの「日誌」をクリックする                                             | 日誌一覧が表示され、メモ一覧が非表示になる | OK   |
| 15  | 「メモ」クリック後、サイドバーの「メモ」リンクがアクティブ（ハイライト）になる | 「メモ」リンクが白色でハイライトされる     | OK   |

### シナリオ 5：エラーハンドリング

| No. | 手順                                       | 期待結果                                              | 結果 |
| --- | ------------------------------------------ | ----------------------------------------------------- | ---- |
| 16  | Spring Boot を停止した状態でアクセスする   | "読み込みに失敗しました" のエラートーストが表示される | OK   |
| 17  | Spring Boot を停止した状態で保存操作を行う | "作成に失敗しました" のエラートーストが表示される     | OK   |
